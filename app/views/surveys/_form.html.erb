<%= form_with(model: survey, local: true) do |form| %>
  <% if survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, "Titre" %>
    <%= form.text_field :title, size: 100, :required => true %>
  </div>

  <div class="field">
    <%= form.label :abstract, "Description" %>
    <%= form.text_area :abstract, rows: 10, cols: 60, :required => true %>
  </div>

  <div class="actions">
    <%= form.submit "#{button}", :class => "button" %>
  </div>
<% end %>
