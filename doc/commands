The first time :
docker-compose run web rails db:create_database

rails generate scaffold Survey title:string abstract:text
docker exec -it progedo_db_1_edaa4e08bab3 psql myapp_development -U postgres

docker-compose up --build

docker-compose run web rails g migration

docker initial setup based on https://docs.docker.com/compose/rails/

# -- Install devise gem
$ bundle
$ rails g devise:install
